# Core Web Application Dependencies
# Install with: pip install -r requirements.txt

Flask==3.0.0
Flask-CORS==4.0.0
requests==2.31.0

# ML dependencies (app.py loads trained models)
-r requirements-ml.txt
